#include <iostream>
using namespace std;

int reduce (int num,int denom,int devisor)
{
  int NumGCD = num/devisor;
  int DenomGCD = denom/devisor;
 if(NumGCD<=0)
 {
    cout <<0;
 }
 else if(DenomGCD<=0)
 {
    cout<<0;
 }
 else
 {
    cout<<1;
 };
}

int main ()
{
  reduce(3,9,1);
}
